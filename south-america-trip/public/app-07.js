"use strict";
function closeDay(){activeDayId=null;$("#detail")?.classList.remove("open");}
function timeline(label,items){if(!items?.length)return"";return `<div class="timeline"><h4>${label}</h4>${items.map(x=>`<div>• ${esc(x)}</div>`).join("")}</div>`;}

function openModal(html){const m=$("#modal");m.innerHTML=`<div class="modalcard">${html}</div>`;m.classList.add("open");return m;}
function closeModal(){$("#modal")?.classList.remove("open");}
function editDay(id){const existing=id?byId(doc.days,id):null;const d=existing?clone(existing):{id:uid("day"),date:doc.tripEnd,title:"New day",location:"",country:"",anchor:"",intro:"",morning:[],afternoon:[],evening:[],eat:[],logistics:[],watchOut:"",notes:"",places:[],travellerIds:["sahil"],locked:false};const f=existing?flightForDay(existing):null;const m=openModal(`<h3>${existing?"Edit day":"Add day"}</h3><form id="dayForm" class="formgrid"><div class="twocol"><label>Date<input class="field" name="date" type="date" value="${esc(d.date)}"></label><label>Title<input class="field" name="title" value="${esc(d.title)}"></label></div><div class="twocol"><label>City<input class="field" name="location" value="${esc(d.location)}"></label><label>Country<input class="field" name="country" value="${esc(d.country)}"></label></div><label>Anchor<textarea class="field area" name="anchor">${esc(d.anchor||"")}</textarea></label><label>Watch-out<textarea class="field area" name="watchOut">${esc(d.watchOut||"")}</textarea></label><label>Shared notes<textarea class="field area" name="notes">${esc(d.notes||"")}</textarea></label><label>Places / map pins · one per line<textarea class="field area" name="places">${esc((d.places||[]).join("\n"))}</textarea></label>${["morning","afternoon","evening","eat","logistics"].map(k=>`<label>${k[0].toUpperCase()+k.slice(1)} · one item per line<textarea class="field area" name="${k}">${esc((d[k]||[]).join("\n"))}</textarea></label>`).join("")}<div><div class="eyebrow">Travellers</div><div class="chips">${TRAVELLERS.map(n=>`<label class="chip"><input type="checkbox" name="traveller" value="${n.toLowerCase()}" ${d.travellerIds?.includes(n.toLowerCase())?"checked":""}> ${n}</label>`).join("")}</div></div><div class="row" style="justify-content:flex-end"><button class="btn ghost" type="button" data-cancel>Cancel</button>${existing?`<button class="btn danger" type="button" data-delete>Delete</button>`:""}<button class="btn primary" type="submit">Save</button></div></form>`);$("[data-cancel]",m).onclick=closeModal;$("#dayForm",m).onsubmit=e=>{e.preventDefault();const fd=new FormData(e.target);const newDate=fd.get("date");if(f?.locked&&newDate!==d.date){toast("This date has locked transport. Unlock/edit that booking first.",true);return;}for(const k of ["date","title","location","country","anchor","watchOut","notes"])d[k]=String(fd.get(k)||"").trim();for(const k of ["places","morning","afternoon","evening","eat","logistics"])d[k]=String(fd.get(k)||"").split("\n").map(x=>x.trim()).filter(Boolean);d.travellerIds=fd.getAll("traveller").map(String);if(existing)Object.assign(existing,d);else doc.days.push(d);doc.tripStart=doc.days.map(x=>x.date).sort()[0]||doc.tripStart;doc.tripEnd=doc.days.map(x=>x.date).sort().at(-1)||doc.tripEnd;closeModal();scheduleSave(existing?`Edited ${d.title}`:`Added ${d.title}`);renderAll();};$("[data-delete]",m)?.addEventListener("click",()=>{if(f?.locked||existing.locked){toast("This day is protected by a locked booking. Unlock the booking first.",true);return;}removeById(doc.days,existing.id);closeModal();closeDay();scheduleSave(`Deleted ${existing.title}`);renderAll();});}
function editTransport(id){const f=byId(doc.flights,id);if(!f)return;const m=openModal(`<h3>Edit transport</h3><form id="transportForm" class="formgrid"><div class="twocol"><label>Date<input class="field" name="date" type="date" value="${esc(f.date)}"></label><label>Status<input class="field" name="status" value="${esc(f.status||"")}"></label></div><label>Route<input class="field" name="route" value="${esc(f.route)}"></label><div class="twocol"><label>Carrier / mode<input class="field" name="mode" value="${esc(f.mode||"")}"></label><label>Timing<input class="field" name="timing" value="${esc(f.timing||"")}"></label></div><label>Cabin baggage<input class="field" name="baggage" value="${esc(f.baggage||"")}"></label><label>Notes<textarea class="field area" name="note">${esc(f.note||"")}</textarea></label><label class="chip"><input type="checkbox" name="locked" ${f.locked?"checked":""}> Locked / purchased</label><div class="row" style="justify-content:flex-end"><button type="button" class="btn ghost" data-cancel>Cancel</button><button class="btn primary">Save</button></div></form>`);$("[data-cancel]",m).onclick=closeModal;$("#transportForm",m).onsubmit=e=>{e.preventDefault();const fd=new FormData(e.target);for(const k of ["date","status","route","mode","timing","baggage","note"])f[k]=String(fd.get(k)||"").trim();f.locked=fd.get("locked")==="on";closeModal();scheduleSave(`Edited transport: ${f.route}`);renderAll();};}
function editStay(id){const s=byId(doc.stays,id);if(!s)return;const m=openModal(`<h3>Edit stay</h3><form id="stayForm" class="formgrid"><div class="twocol"><label>City<input class="field" name="city" value="${esc(s.city)}"></label><label>Dates<input class="field" name="dates" value="${esc(s.dates||"")}"></label></div><label>Area<input class="field" name="area" value="${esc(s.area||"")}"></label><label>Notes<textarea class="field area" name="note">${esc(s.note||"")}</textarea></label><label>Hotel options · one per line<textarea class="field area" name="options">${esc((s.options||[]).map(x=>x.name).join("\n"))}</textarea></label><label class="chip"><input type="checkbox" name="locked" ${s.locked?"checked":""}> Locked / purchased</label><div class="row" style="justify-content:flex-end"><button type="button" class="btn ghost" data-cancel>Cancel</button><button class="btn primary">Save</button></div></form>`);$("[data-cancel]",m).onclick=closeModal;$("#stayForm",m).onsubmit=e=>{e.preventDefault();const fd=new FormData(e.target);for(const k of ["city","dates","area","note"])s[k]=String(fd.get(k)||"").trim();const old=new Map((s.options||[]).map(x=>[x.name,x]));s.options=String(fd.get("options")||"").split("\n").map(x=>x.trim()).filter(Boolean).map(name=>old.get(name)||{id:uid("hotel"),name});s.locked=fd.get("locked")==="on";closeModal();scheduleSave(`Edited stay: ${s.city}`);renderAll();};}
